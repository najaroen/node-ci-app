const { expect } = require('chai');


describe('Math functions', () => {
  it('should add two numbers correctly', () => {
    expect("Hello How long").to.equal("Hello");
  });

  it('should multiply two numbers correctly', () => {
    // expect(multiply(4, 5)).to.equal(20);
  });
});
